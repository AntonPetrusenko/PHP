version: '3.6'

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./src:/var/www/public
      - ./nginx/nginx.conf:/etc/nginx/conf.d/nginx.conf
    depends_on:
      - php

  php:
    build: ./php
#    image: php:7.3-fpm
    ports:
      - "9000:9000"
    volumes:
      - ./src:/var/www/public
    depends_on:
      - mysql

  mysql:
    image: mysql
    restart: always
    volumes:
      - "mysql:/var/lib/mysql"
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: example

volumes:
  mysql:
